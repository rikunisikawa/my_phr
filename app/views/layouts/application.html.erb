<!DOCTYPE html>
<html>
  <head>
    <title>RailsPhr</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined" />
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <div class="container-fluid">
      <div class="row w-100">
        <nav id="sidebar" class="col-3 bg-primary">
            <div class="d-flex flex-column">
              <%= button_to "プロフィール", profiles_path, class: "btn btn-primary border text-center my-1", style: "height: 50px; line-height: 50px; box-shadow: none; width: 100%;" %>
              <%= button_to "健康記録", health_records_path, class: "btn btn-primary border text-center my-1", style: "height: 50px; line-height: 50px; box-shadow: none; width: 100%;" %>
              <%= button_to "サインアップ", new_user_registration_path, class: "btn btn-primary border text-center my-1", style: "height: 50px; line-height: 50px; box-shadow: none; width: 100%;" %>
              <%= button_to "ログイン", new_user_session_path, class: "btn btn-primary border text-center my-1", style: "height: 50px; line-height: 50px; box-shadow: none; width: 100%;" %>
            </div>
        </nav>
        <main class="col-9">
          <nav class="navbar navbar-expand-lg navbar-dark bg-primary md-shadow">
            <div class="container-fluid">
              <a class="btn btn-light" href="/">ホーム</a>
            </div>
          </nav>
          <%= yield %>
        </main>
      </div>
    </div>
  </body>
</html>
